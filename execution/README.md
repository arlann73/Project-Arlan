# Execution (Layer 3)

This directory holds deterministic Python execution scripts.

*   Each script is a tool the agent can call to perform a specific task reliably.
*   Scripts should be well-commented, testable, and use environment variables from `../.env`.
*   Handle API calls, data processing, file operations, database interactions here.
