 {\r\n165:             const projectSection = document.getElementById('projects');\r\n166:             const sidebarCards = document.querySelectorAll('.sidebar-card');\r\n167: \r\n168:             function updateSidebarVisibility() {\r\n169:                 const heroBottom = heroSection.getBoundingClientRect().bottom;\r\n170:                 if (heroBottom > 0) {\r\n171:                     sidebarEl.classList.add('sidebar--hidden');\r\n172:                 } else {\r\n173:                     sidebarEl.classList.remove('sidebar--hidden');\r\n174:                 }\r\n175: \r\n176:                 // Individually invert sidebar cards when they cross into the projects section\r\n177:                 if (projectSection) {\r\n178:                     const projRect = projectSection.getBoundingClientRect();\r\n179:                     sidebarCards.forEach(card => {\r\n180:                         const cardRect = card.getBoundingClientRect();\r\n181:                         const cardCenter = cardRect.top + (cardRect.height / 2);\r\n182:                         if (cardCenter >= projRect.top && cardCenter <= projRect.bottom) {\r\n183:                             card.classList.add('is-inverted');\r\n184:                         } else {\r\n185:                             card.classList.remove('is-inverted');\r\n186:                         }\r\n187:                     });\r\n188:                 }\r\n189:             }\r\n190:             updateSidebarVisibility();\r\n191:             window.addEventListener('scroll', updateSidebarVisibility);\r\n192:         }\r\n193: \r\n194:         // 3. Hero nav link visibility (single centered bar)\r\n195:         const heroNav = document.getElementById('hero-nav');\r\n196: \r\n197:         // Load animation config (if present) and provide helper\r\n198:         var _animCfg = { heroNav: { xLeft: -100, xRight: 100, duration: 0.5 }, curve: { scrub: 1 }, transform: { scrub: true }, swiper: { slidesPerView: 'auto', spaceBetween: 3